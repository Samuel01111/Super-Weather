package com.example.superweather.data.firebase

class FirebaseParams {
    companion object {
        const val URL = "url"
        const val HEADERS = "headers"
        const val METHOD = "method"
        const val BODY = "body"
        const val CODE = "code"
    }
}